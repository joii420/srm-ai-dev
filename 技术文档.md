# Appsmith AI-IDE 技术框架设计

| 字段 | 内容 |
|------|------|
| 文档版本 | V5.0 |
| 创建日期 | 2026-03-23 |
| 文档状态 | 正式稿 |
| 参考 PRD 版本 | V6.0 |
| 目标读者 | 前端工程师 / 后端工程师 / DevOps |

---

## 修订记录

| 版本 | 日期 | 变更说明 |
|------|------|----------|
| V4.0 | 2026-03-23 | 初始版本（基于本地裸仓库方案） |
| V5.0 | 2026-03-23 | 对齐 PRD V6.0：Git 架构改为内网 GitLab（SSH）；ORM 由 Drizzle 改为 Prisma；新增 SSH Key 注入机制；补充用户认证 Schema；新增系统配置表；依赖库接口归入 ai-proxy；修正 docs 目录命名 |

---

## 1. 整体架构

系统由三层构成：用户浏览器（React Web IDE）、后端管理服务（Node.js + Fastify）、以及按需创建的 Docker 容器（含 AI Proxy 和 File Manager 双服务）。Git 版本控制托管于内网 GitLab，每个 Appsmith Page 拥有独立的 GitLab 仓库。

| 层级 | 技术 | 职责 |
|------|------|------|
| 用户浏览器 | React 18 + Vite + TypeScript | 签出/签入 · IDE 工作区 · 依赖库管理 · Skill 配置 |
| 后端管理服务 | Node.js 20 + Fastify | Page 管理 · Docker 生命周期 · Skill/Deps 配置 · 用户认证 |
| Docker 容器 | Node.js 20 Alpine + PM2 | Claude AI Proxy(:3000) · File Manager(:3001) · /workspace/ · /deps/ |
| 数据库 | PostgreSQL 16 | users · skills · dependencies · checkouts · system_config |
| Git 仓库 | 内网 GitLab | 每 Page 独立仓库（dev 分支）· ai-ide-project 主项目仓库 |

### 1.1 通信协议

| 链路 | 协议 | 说明 |
|------|------|------|
| 浏览器 → 后端 | HTTP REST / SSE | 标准 JSON 接口；AI 流式回复通过 SSE 推送 |
| 后端 → Docker | Docker SDK (dockerode) | 容器生命周期管理 |
| 后端 → GitLab | SSH (git) | 签出克隆、签入推送 |
| 后端 → PostgreSQL | TCP | Prisma 连接池 |
| 浏览器 → 容器 | HTTP（经后端代理） | 文件读写、AI 对话 |

---

## 2. Monorepo 结构

项目使用 pnpm workspaces 管理三个子包，代码托管在内网 GitLab 的 ai-ide-project 仓库。

### 2.1 根目录结构

```
appsmith-ai-ide/
├── packages/
│   ├── frontend/            ← Web IDE 前端
│   ├── backend/             ← 管理服务后端
│   └── container-services/  ← Docker 容器内双服务
├── docker/
│   ├── Dockerfile.container
│   ├── docker-compose.dev.yml
│   └── pm2.config.js
├── scripts/
│   └── setup.sh
├── .nvmrc                   ← Node v20
├── pnpm-workspace.yaml
└── package.json
```

---

## 3. 各包详细结构

### 3.1 前端（packages/frontend）

```
packages/frontend/src/
├── pages/
│   ├── LoginPage/            ← 登录页（账号密码 + JWT）
│   ├── CheckoutPage/         ← Page 签出选择
│   ├── IDEPage/
│   │   ├── FileTree/
│   │   ├── Editor/
│   │   ├── Chat/
│   │   └── SkillDrawer/
│   ├── DepsPage/
│   ├── SkillsPage/
│   └── SystemConfigPage/     ← 系统配置（仅管理员）
├── components/shared/
├── hooks/
│   ├── useAuth.ts            ← JWT Token 管理、登录状态
│   ├── useEditor.ts
│   ├── useChat.ts
│   └── useContainer.ts
└── services/
    ├── api.ts                ← 后端 REST（统一携带 Authorization Header）
    ├── containerApi.ts
    └── sse.ts
```

### 3.2 后端（packages/backend）

```
packages/backend/
├── prisma/
│   ├── schema.prisma         ← Prisma Schema（数据模型定义）
│   └── migrations/           ← prisma migrate 生成的迁移文件
└── src/
    ├── lib/
    │   └── prisma.ts         ← PrismaClient 单例
    ├── routes/
    │   ├── auth.ts               ← POST /auth/login · /logout · GET /auth/me
    │   ├── pages.ts              ← 签出/签入接口
    │   ├── skills.ts             ← Skill CRUD
    │   ├── deps.ts               ← 依赖库配置
    │   ├── containers.ts         ← 容器状态查询
    │   └── systemConfig.ts       ← 系统配置（仅管理员）
    ├── services/
    │   ├── DockerService.ts      ← Docker SDK 封装
    │   ├── GitService.ts         ← GitLab SSH 操作封装
    │   ├── ContainerLifecycle.ts ← 容器生命周期管理
    │   ├── DepsLoader.ts         ← 依赖库拉取与缓存
    │   └── SshKeyService.ts      ← SSH Key 加密存取
    └── middleware/
        ├── auth.ts               ← JWT 校验中间件
        └── errorHandler.ts
```

### 3.3 容器内服务（packages/container-services）

```
packages/container-services/src/
├── ai-proxy/                 ← Claude AI 代理服务（:3000）
│   ├── routes/
│   │   ├── chat.ts           ← POST /api/chat（SSE 流式输出）
│   │   ├── session.ts        ← GET/DELETE /api/session
│   │   ├── context.ts        ← POST /api/context/refresh
│   │   ├── deps.ts           ← GET /api/deps
│   │   │                        POST /api/deps/{name}/refresh
│   │   │                        POST /api/deps/refresh-all
│   │   └── health.ts         ← GET /api/health
│   └── services/
│       ├── ClaudeClient.ts
│       ├── SessionManager.ts
│       ├── ContextBuilder.ts
│       └── SkillInjector.ts
└── file-manager/             ← 文件管理服务（:3001）
    ├── routes/
    │   └── files.ts          ← GET/POST/PUT/DELETE /files
    └── services/
        └── FileService.ts
```

---

## 4. 技术选型

### 4.1 前端

| 类别 | 技术 | 选型理由 |
|------|------|----------|
| 框架 | React 18 + TypeScript | 生态成熟，组件化开发 |
| 构建 | Vite | 极速 HMR |
| 状态管理 | Zustand | 轻量，无模板代码 |
| 路由 | React Router v6 | 标准方案，支持路由守卫（未登录重定向） |
| 代码编辑器 | Monaco Editor | VS Code 同款引擎，语法高亮完善 |
| 样式 | CSS Modules + CSS Variables | 无运行时开销，主题切换友好 |
| HTTP | Axios + React Query | 请求缓存、错误重试；拦截器统一注入 JWT Token |
| 实时推送 | SSE（EventSource） | AI 流式输出 |

### 4.2 后端

| 类别 | 技术 | 选型理由 |
|------|------|----------|
| 运行时 | Node.js 20 LTS | 与容器服务统一技术栈 |
| 框架 | Fastify | 高性能，TypeScript 原生支持 |
| ORM | Prisma | 类型安全，Schema 即文档；prisma migrate 管理迁移 |
| 数据库 | PostgreSQL 16 | 高可靠，支持 JSONB |
| Docker | dockerode | Node.js Docker SDK |
| Git | simple-git | 封装 git clone/push，通过 SSH Key 访问 GitLab |
| 认证 | jsonwebtoken + bcrypt | JWT 签发；bcrypt 存储密码哈希 |
| 加密 | Node.js crypto (AES-256-GCM) | SSH 私钥加密存储 |
| 参数校验 | Zod | TypeScript 类型推断 |
| 日志 | Pino | 高性能结构化日志 |

### 4.3 容器内服务

| 类别 | 技术 | 说明 |
|------|------|------|
| 运行时 | Node.js 20 Alpine | 镜像体积小 |
| 框架 | Fastify | 与后端统一 |
| AI SDK | @anthropic-ai/sdk | 官方 Claude SDK |
| 流式输出 | SDK stream API + SSE | 对话流式回复 |
| 进程管理 | PM2 | 同时管理 ai-proxy 和 file-manager |

---

## 5. 数据库 Schema（PostgreSQL）

### 5.1 users — 用户表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID PK | gen_random_uuid() |
| username | VARCHAR(100) UNIQUE NOT NULL | 登录账号 |
| password_hash | TEXT NOT NULL | bcrypt 哈希，不存明文 |
| display_name | VARCHAR(100) | 展示名称 |
| role | VARCHAR(20) DEFAULT 'developer' | 'developer' \| 'admin' |
| active_skills | JSONB DEFAULT '[]' | 上次使用的 Skill ID 列表 |
| created_at | TIMESTAMPTZ DEFAULT NOW() | 创建时间 |

### 5.2 skills — Skill 配置表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID PK | |
| name | VARCHAR(100) UNIQUE NOT NULL | Skill 唯一名称 |
| description | TEXT | 用途说明 |
| prompt | TEXT NOT NULL | 注入 AI 的 Prompt 模板 |
| params | JSONB DEFAULT '{}' | 可调参数 |
| tags | JSONB DEFAULT '[]' | 标签数组，如 ["canvas.json","代码质量"] |
| enabled | BOOLEAN DEFAULT true | 是否出现在 IDE 选择器中 |
| version | VARCHAR(20) DEFAULT 'v1.0' | 当前版本号 |
| call_count | INTEGER DEFAULT 0 | 使用次数，用于排序（降序） |
| created_by | UUID REFERENCES users(id) | 创建人 |
| created_at | TIMESTAMPTZ DEFAULT NOW() | |
| updated_at | TIMESTAMPTZ DEFAULT NOW() | |

### 5.3 dependencies — 依赖库配置表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID PK | |
| namespace | VARCHAR(50) UNIQUE NOT NULL | 调用时的变量名，如 com |
| url | TEXT NOT NULL | 库源代码可访问地址 |
| version | VARCHAR(50) | 版本备注 |
| description | TEXT | 用途说明 |
| last_loaded | TIMESTAMPTZ | 上次成功拉取时间 |
| created_at | TIMESTAMPTZ DEFAULT NOW() | |

### 5.4 checkouts — Page 签出记录表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID PK | |
| page_id | VARCHAR(200) NOT NULL | GitLab 仓库标识（如 DashboardPage） |
| page_name | VARCHAR(200) NOT NULL | 展示名称 |
| gitlab_repo_url | TEXT NOT NULL | SSH 格式 GitLab 仓库地址 |
| git_branch | VARCHAR(200) DEFAULT 'dev' | 分支名，默认 dev |
| user_id | UUID REFERENCES users(id) | 签出人 |
| container_id | VARCHAR(100) | Docker 容器 ID |
| session_id | VARCHAR(100) | AI 会话 ID |
| status | VARCHAR(20) DEFAULT 'active' | 'active' \| 'checkedin' |
| checked_out_at | TIMESTAMPTZ DEFAULT NOW() | 签出时间 |
| checked_in_at | TIMESTAMPTZ | 签入时间 |

### 5.5 system_config — 系统配置表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID PK | |
| key | VARCHAR(100) UNIQUE NOT NULL | 配置键，如 container.memoryLimit |
| value | JSONB NOT NULL | 配置值（支持字符串/数字/数组） |
| description | TEXT | 说明 |
| updated_by | UUID REFERENCES users(id) | 最后修改人 |
| updated_at | TIMESTAMPTZ DEFAULT NOW() | |

**系统配置默认值：**

| 配置键 | 默认值 | 说明 |
|--------|--------|------|
| container.memoryLimit | "1g" | 单容器内存上限 |
| container.cpuLimit | 1.0 | 单容器 CPU 上限（核数） |
| container.maxConcurrent | 20 | 最大并发容器数 |
| container.healthCheckTimeout | 60 | 健康检查超时（秒） |
| claude.disabledTools | ["WebFetch","WebSearch"] | 默认禁用的工具 |
| claude.allowedPaths | ["/workspace/**","/deps/**"] | AI 可读写路径白名单 |
| claude.deniedPaths | ["/run/secrets/**","/etc/**"] | AI 明确禁止路径 |
| auth.jwtExpiresIn | "8h" | JWT Token 有效期 |
| deps.urlWhitelist | [] | 依赖库 URL 前缀白名单（空=允许所有） |
| git.gitlabDomain | "" | 内网 GitLab 域名，用于 known_hosts |

> SSH 私钥使用 AES-256-GCM 加密后存储于独立的安全存储中（环境变量或密钥管理服务），不存入 system_config 表明文字段。

---

## 6. 用户认证设计

### 6.1 认证流程

所有后端接口（`/auth/login` 除外）统一校验 JWT Token，Token 通过 `Authorization: Bearer <token>` Header 传递。

| 步骤 | 说明 |
|------|------|
| 1. 登录 | 前端 POST /auth/login { username, password } |
| 2. 密码校验 | 后端 bcrypt.compare() 校验密码哈希 |
| 3. 签发 Token | jsonwebtoken.sign({ userId, role }, secret, { expiresIn: "8h" }) |
| 4. 前端存储 | Token 存储于 localStorage，axios 拦截器统一注入 Authorization Header |
| 5. 接口鉴权 | 中间件解码 Token，注入 { userId, role } 至请求上下文 |
| 6. 权限控制 | 管理员接口检查 role === 'admin'，否则返回 403 |
| 7. Token 过期 | 后端返回 401，前端清除 localStorage Token，跳转登录页 |

### 6.2 认证接口

| 接口 | Method | 权限 | 说明 |
|------|--------|------|------|
| /auth/login | POST | 公开 | 返回 { token, userInfo } |
| /auth/logout | POST | 已登录 | 服务端可加入 Token 黑名单（可选） |
| /auth/me | GET | 已登录 | 返回当前登录用户信息 |
| /auth/users | GET | 管理员 | 用户列表 |
| /auth/users | POST | 管理员 | 创建用户 |
| /auth/users/:id/password | PUT | 管理员 | 重置密码 |
| /auth/users/:id | DELETE | 管理员 | 删除用户 |

---

## 7. SSH Key 与 GitLab 集成

### 7.1 SSH Key 安全机制

| 环节 | 方案 |
|------|------|
| 存储 | 管理员上传 PEM 格式私钥，后端使用 AES-256-GCM 加密后存储，密钥不存入数据库明文字段 |
| 注入 | 容器创建时，将私钥解密后写入容器 tmpfs（/run/secrets/id_git，权限 600） |
| 使用 | 容器内 git clone/push 使用 GIT_SSH_COMMAND 环境变量指向 /run/secrets/id_git |
| 清除 | 容器销毁后，tmpfs 随内存自动清除，私钥不落盘 |
| AI 隔离 | claude.deniedPaths 配置明确拒绝 AI 读取 /run/secrets/** 路径 |

### 7.2 Git 仓库结构

```
GitLab（内网）
├── DashboardPage     ← dev 分支，独立仓库
│   └── DashboardPage/
│       ├── canvas.json
│       └── JsObjects/
│           ├── COMMON.js
│           └── PROCESS.js
├── UserPage          ← dev 分支，独立仓库
├── ...               ← 其他 Page 各自独立
└── ai-ide-project    ← 本项目代码仓库
```

- **签出** = `git clone -b dev git@gitlab.internal:<group>/<PageName>.git` → 容器 `/workspace/`
- **签入** = `git push origin dev` → GitLab 对应仓库，容器随后销毁

### 7.3 容器启动环境变量

| 变量 | 示例值 | 说明 |
|------|--------|------|
| CLAUDE_API_KEY | sk-ant-... | Anthropic API Key |
| PAGE_ID | DashboardPage | Page 标识 |
| SESSION_ID | abc123 | AI 会话 ID |
| GITLAB_REPO_URL | git@gitlab.internal:group/DashboardPage.git | SSH 格式仓库地址 |
| GIT_BRANCH | dev | 克隆分支 |
| DEP_URLS | com=https://cdn.example.com/com.js | 依赖库命名空间=URL |
| SSH_KEY_PATH | /run/secrets/id_git | 注入的 SSH 私钥路径 |

---

## 8. 签出 / 签入核心流程

### 8.1 签出流程（含 SSH Key 注入）

| 步骤 | 执行方 | 描述 |
|------|--------|------|
| 1 | 后端 | 校验 JWT Token；校验 Page 未被签出；标记 Page 为「已签出」，记录签出人 |
| 2 | 后端 | 从加密存储读取 SSH 私钥，解密 |
| 3 | 后端 | 调用 Docker API 创建容器；将私钥写入容器 tmpfs（/run/secrets/id_git，权限 600） |
| 4 | 容器 | 执行 git clone -b dev <gitlab-repo-url>，代码拉取至 /workspace/ |
| 5 | 容器 | 按依赖库配置拉取所有外部库代码至 /deps/ |
| 6 | 容器 | 启动 Claude AI 服务，注入当前 Page 仓库 + 依赖库完整上下文 + Skill Prompt |
| 7 | 后端 | 轮询 /api/health，健康检查通过（超时 60s 则回滚并解锁 Page） |
| 8 | 前端 | 跳转至 IDE 工作区，加载当前 Page 文件树 |

### 8.2 签入流程

| 步骤 | 执行方 | 描述 |
|------|--------|------|
| 1 | 容器 | git add . && git commit -m "\<message\>" && git push origin dev → GitLab |
| 2 | 后端 | 停止并删除 Docker 容器（容器内 /workspace/ 代码随容器销毁，tmpfs 自动清除） |
| 3 | 后端 | 解除 Page 签出锁定，状态恢复「空闲」，更新 checkouts 记录 |
| 4 | 前端 | 跳回 Page 签出页 |

### 8.3 异常处理

| 异常 | 处理方式 |
|------|----------|
| 容器启动超时（>60s） | 自动回滚：停止容器、清理 tmpfs、解除 Page 锁定，提示用户重试 |
| git clone 失败（SSH Key 无效/网络异常） | 回滚，提示检查 SSH Key 配置或 GitLab 连通性 |
| git push 失败（冲突/权限） | 提示用户，容器保留，用户处理后可重试签入 |
| GitLab 不可用 | 提示用户，容器保留，等待 GitLab 恢复后重试 |
| 并发容器超出上限 | 拒绝新签出请求，提示用户等待 |

---

## 9. 容器镜像

### 9.1 Dockerfile

```dockerfile
# docker/Dockerfile.container
FROM node:20-alpine

RUN apk add --no-cache git openssh-client

WORKDIR /app
COPY packages/container-services/package.json ./
RUN npm install --production
COPY packages/container-services/dist ./dist

RUN npm install -g pm2
RUN mkdir -p /workspace /deps /run/secrets

COPY docker/pm2.config.js ./
EXPOSE 3000 3001
CMD ["pm2-runtime", "pm2.config.js"]
```

> `openssh-client` 提供 ssh 命令，用于 git 通过 SSH Key 访问 GitLab。

### 9.2 PM2 配置

```javascript
// docker/pm2.config.js
module.exports = {
  apps: [
    {
      name: 'ai-proxy',
      script: './dist/ai-proxy/app.js',
      env: { PORT: 3000 }
    },
    {
      name: 'file-manager',
      script: './dist/file-manager/app.js',
      env: { PORT: 3001, WORKSPACE: '/workspace' }
    }
  ]
};
```

### 9.3 容器安全加固

| 措施 | 说明 |
|------|------|
| 非 root 用户 | 容器以 node 用户（UID 1000）运行，不以 root 启动 |
| no-new-privileges | 防止子进程通过 setuid 提权 |
| ReadonlyRootfs | 根文件系统只读，/workspace /deps /run/secrets 单独挂载可写 |
| 网络隔离 | 禁止容器间通信；仅允许访问 GitLab 域名和 Anthropic API |
| tmpfs 私钥 | /run/secrets 使用 tmpfs，容器销毁即清除，不写入宿主机磁盘 |
| AI 路径白名单 | claude.allowedPaths 限制 AI 只能读写 /workspace 和 /deps |

---

## 10. 容器内接口规范

### 10.1 AI Proxy（:3000）

| 接口 | Method | 说明 |
|------|--------|------|
| /api/health | GET | 健康检查，签出时后端轮询 |
| /api/chat | POST | 发送消息，SSE 流式返回 AI 回复 |
| /api/session | GET | 获取当前会话信息 |
| /api/session | DELETE | 清理会话 |
| /api/context/refresh | POST | 刷新 AI 上下文（重读仓库 + 依赖库） |
| /api/deps | GET | 获取已加载依赖库列表及版本 |
| /api/deps/:name/refresh | POST | 更新指定依赖库并刷新上下文 |
| /api/deps/refresh-all | POST | 更新全部依赖库并刷新上下文 |

`/api/chat` 请求体：

```json
{
  "message": "给 fetchData 函数添加错误处理",
  "currentFile": "JsObjects/PROCESS.js",
  "fileContent": "<当前文件完整内容>",
  "skills": ["error-handle", "code-review"]
}
```

> 对话历史由容器内 ClaudeClient 服务端管理，前端无需传递 `history` 字段。

### 10.2 File Manager（:3001）

| 接口 | Method | 说明 |
|------|--------|------|
| /files | GET | 获取文件树 |
| /files/{path} | GET | 读取文件内容 |
| /files/{path} | POST | 创建文件 |
| /files/{path} | PUT | 更新文件内容（保存） |
| /files/{path} | DELETE | 删除文件 |
| /files/diff | GET | 获取 git diff（所有未提交变更） |

---

## 11. 后端接口规范（管理服务 :3100）

所有接口（`/auth/login` 除外）需携带 `Authorization: Bearer <token>` Header。

| 接口 | Method | 权限 | 说明 |
|------|--------|------|------|
| /auth/login | POST | 公开 | 账号密码登录，返回 JWT Token |
| /auth/me | GET | 已登录 | 当前用户信息 |
| /auth/logout | POST | 已登录 | 登出 |
| /auth/users | GET/POST | 管理员 | 用户列表 / 创建用户 |
| /auth/users/:id | PUT/DELETE | 管理员 | 修改 / 删除用户 |
| /pages | GET | 已登录 | 所有 Page 列表（含状态） |
| /pages/:id/checkout | POST | 已登录 | 签出 Page |
| /pages/:id/checkin | POST | 已登录 | 签入 Page |
| /skills | GET/POST | 已登录/管理员 | Skill 列表（按 call_count 降序）/ 创建 |
| /skills/:id | PUT/DELETE | 管理员 | 编辑 / 删除 Skill |
| /skills/:id/use | POST | 已登录 | call_count +1 |
| /deps | GET/POST | 已登录/管理员 | 依赖库列表 / 添加 |
| /deps/:id | PUT/DELETE | 管理员 | 编辑 / 删除依赖库 |
| /containers/:id/status | GET | 已登录 | 容器状态查询 |
| /system-config | GET | 已登录 | 获取系统配置（脱敏） |
| /system-config | PUT | 管理员 | 更新系统配置 |
| /system-config/ssh-key | PUT | 管理员 | 上传/更新 SSH 私钥 |
| /system-config/ssh-test | POST | 管理员 | SSH 连接测试 |

---

## 12. 本地开发环境

### 12.1 docker-compose 编排

```yaml
# docker/docker-compose.dev.yml
services:
  postgres:
    image: postgres:16-alpine
    ports: ["5432:5432"]
    environment:
      - POSTGRES_DB=ai_ide_dev
      - POSTGRES_USER=ai_ide
      - POSTGRES_PASSWORD=dev_password
    volumes:
      - pg_data:/var/lib/postgresql/data

  frontend:
    build: { context: ../packages/frontend, target: dev }
    ports: ["5173:5173"]
    volumes:
      - ../packages/frontend/src:/app/src

  backend:
    build: { context: ../packages/backend, target: dev }
    ports: ["3100:3100"]
    depends_on: [postgres]
    volumes:
      - ../packages/backend/src:/app/src
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - DATABASE_URL=postgresql://ai_ide:dev_password@postgres:5432/ai_ide_dev
      - CLAUDE_API_KEY=${CLAUDE_API_KEY}
      - SSH_KEY_ENCRYPT_SECRET=${SSH_KEY_ENCRYPT_SECRET}
      - GITLAB_KNOWN_HOSTS=${GITLAB_KNOWN_HOSTS}

volumes:
  pg_data:
```

### 12.2 快速启动

```bash
# 1. 克隆项目
git clone git@gitlab.internal:group/ai-ide-project.git
cd appsmith-ai-ide

# 2. 安装依赖
pnpm install

# 3. 配置环境变量
cp .env.example .env
# 填写: CLAUDE_API_KEY / SSH_KEY_ENCRYPT_SECRET / GITLAB_KNOWN_HOSTS

# 4. 数据库迁移
pnpm --filter backend db:migrate   # prisma migrate dev
pnpm --filter backend db:generate  # 生成 Prisma Client

# 5. 启动开发环境
pnpm dev

# 6. 构建容器镜像
pnpm docker:build
```

### 12.3 根 package.json 脚本

| 脚本 | 说明 |
|------|------|
| pnpm dev | docker-compose -f docker/docker-compose.dev.yml up |
| pnpm dev:fe | 单独启动前端开发服务器 |
| pnpm dev:be | 单独启动后端开发服务器 |
| pnpm build | 编译所有子包 |
| pnpm test | 运行所有子包测试 |
| pnpm lint | 运行所有子包 ESLint |
| pnpm docker:build | 构建容器镜像（需先 build container-services） |
| pnpm db:migrate | prisma migrate dev（开发）/ prisma migrate deploy（生产） |
| pnpm db:studio | 启动 Prisma Studio 可视化查看数据库 |
| pnpm db:generate | prisma generate，更新 Prisma Client 类型 |

---

## 13. Git 规范

### 13.1 分支策略

```
main       ← 稳定分支，对应线上版本
develop    ← 集成分支，所有功能合入此处
  ├── feature/<功能名>
  ├── fix/<问题描述>
  └── docs/<描述>
```

### 13.2 Commit 规范（Conventional Commits）

格式：`<type>(<scope>): <subject>`

| type | 说明 |
|------|------|
| feat | 新功能 |
| fix | Bug 修复 |
| refactor | 重构 |
| docs | 文档变更 |
| chore | 构建 / 工具链变更 |
| perf | 性能优化 |

| scope | 对应模块 |
|-------|----------|
| fe | packages/frontend |
| be | packages/backend |
| cs | packages/container-services |
| docker | docker/ |

示例：

```
feat(fe): add login page with JWT token storage
feat(be): add SSH key encryption and container injection
```

### 13.3 Git Hooks（husky）

| Hook | 规则 |
|------|------|
| pre-commit | lint-staged（ESLint + Prettier，只扫描变更文件）；禁止直接 commit 到 main |
| commit-msg | commitlint（校验 Conventional Commits 格式） |

---

## 14. 目录速查

```
appsmith-ai-ide/
├── packages/
│   ├── frontend/              React 18 + Vite + TypeScript
│   │   └── src/pages/         Login · Checkout · IDE · Deps · Skills · SystemConfig
│   ├── backend/               Node.js 20 + Fastify + Prisma
│   │   ├── prisma/            schema.prisma · migrations/
│   │   └── src/
│   │       ├── routes/        auth · pages · skills · deps · containers · systemConfig
│   │       ├── services/      Docker · Git(GitLab SSH) · DepsLoader · SshKey · ContainerLifecycle
│   │       └── lib/           prisma.ts（PrismaClient 单例）
│   └── container-services/    容器内双服务
│       └── src/
│           ├── ai-proxy/      :3000  chat · session · context · deps · health
│           └── file-manager/  :3001  文件 CRUD + git diff
├── docker/
│   ├── Dockerfile.container   node:20-alpine + git + openssh-client + PM2
│   ├── docker-compose.dev.yml 本地全栈开发编排
│   └── pm2.config.js          ai-proxy + file-manager 双进程
└── package.json               dev · build · docker:build · db:migrate · db:studio · db:generate
```

---

*文档结束 · V5.0 · 2026-03-23 · 对齐 PRD V6.0*
